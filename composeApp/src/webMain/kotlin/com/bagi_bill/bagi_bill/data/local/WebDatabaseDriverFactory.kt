package com.bagi_bill.bagi_bill.data.local

/**
 * Web platform doesn't fully support SQLDelight web-worker-driver for WASM yet.
 * Use WebBillRepository with in-memory + localStorage persistence instead.
 */
object WebDatabaseHelper {
    // Placeholder for potential future web database support
}

